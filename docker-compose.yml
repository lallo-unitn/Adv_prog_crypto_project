version: '3'
services:
  #datasource:
    #image: postgres:latest
      # build: ./services/datasource
      #container_name: datasource
      #network_mode: host
      #restart: always
      #stdin_open: true
      #tty: true
      #ports:
      # - 5432:5432
      #environment: 
      #- POSTGRES_DB=universityDatabase
      #- POSTGRES_USER=lallo
      #- POSTGRES_PASSWORD=lallo
      #volumes:
      #- ./services/datasource/init.sql:/docker-entrypoint-initdb.d/init.sql

  application_server:
    image: quay.io/wildfly/wildfly:30.0.0.Final-jdk17
      #build: ./services/application_server
    command: >
      bash -c "/opt/jboss/wildfly/bin/add-user.sh admin admin --silent
      && /opt/jboss/wildfly/bin/standalone.sh -b 0.0.0.0 -bmanagement 0.0.0.0"
    container_name: application_server
    network_mode: host
    restart: always
    stdin_open: true
    tty: true
    ports:
      - 8080:8080
      - 9090:9090
    environment:
      - WILDFLY_PASS=admin
      - WILDFLY_USER=admin
      - WILDFLY_PASSWORD=admin 
        #volumes:
        #- ./services/application_server/deployments:/opt/wildfly/standalone/deployments
        #- configuration:/opt/wildfly/standalone/configuration
        #- ./services/application_server/modules/postgresql:/opt/wildfly/modules/org/postgresql

    volumes:
      - type: bind
        source: ./services/application_server/conf2/standalone.xml
        target: /opt/jboss/wildfly/standalone/configuration/standalone.xml
      - type: bind
        source: ./services/application_server/modules/org/postgresql
        target: /opt/jboss/wildfly/modules/org/postgresql
      - type: bind
        source: ./services/application_server/modules/com/mariadb
        target: /opt/jboss/wildfly/modules/com/mariadb
      - type: bind
        source: ./services/application_server/deployments/adv_prog_project_war.war
        target: /opt/jboss/wildfly/standalone/deployments/adv_prog_project_war.war

  mariadb:
    image: mariadb:latest
    restart: always
    network_mode: host
    environment:
      #MARIADB_USER: user
      #MARIADB_PASSWORD: user
      MARIADB_ROOT_PASSWORD: root
    ports:
      - 3306:3306
    volumes:
      - ./services/mariadb/docker-entrypoint-initdb.d/init.sql:/docker-entrypoint-initdb.d/init.sql
      - type: bind
        source: ./services/mariadb/mysql/conf.d/server.cnf
        target: /etc/mysql/conf.d/server.cnf
      - type: bind
        source: ./services/mariadb/mysql/encryption
        target: /etc/mysql/conf.d/encryption

          # adminer:
          #image: adminer:latest
          #restart: always
          #network_mode: host
          #ports:
          #- 8090:8080

volumes:
  configuration:
    name: configuration
  

          # car-sharing:
          #   image: car-sharing:latest
          #   build: ./service/car-sharing
          #   container_name: car-sharing
          #   restart: unless-stopped
          #   stdin_open: true
          #   tty: true
          #   networks:
          #     - tech-service

          # laundry-scheduler:
          #   image: laundry-scheduler:latest
          #   build: ./service/laundry-scheduler
          #   container_name: laundry-scheduler
          #   restart: unless-stopped
          #   stdin_open: true
          #   tty: true
          #   networks:
          #     - tech-service

          # nest-cooking-schedule:
          #   image: nest-cooking-schedule:latest
          #   build: ./service/nest-cooking-schedule
          #   container_name: nest-cooking-schedule
          #   restart: unless-stopped
          #   stdin_open: true
          #   tty: true
          #   networks:
          #     - tech-service 

